"""Configuration schema for Dashview V2."""

import voluptuous as vol

# Configuration schema (will be expanded in future)
DashviewConfigSchema = vol.Schema(
    {
        # Future configuration options will go here
    }
)